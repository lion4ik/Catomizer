package com.github.catomizer.data.network.model

//TODO: It would be better to separate api model and ui model, but they will be the same. That's why I leave it as it for now.
data class CatApiModel(val id: String, val url: String)